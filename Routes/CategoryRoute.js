const express = require("express");
const router = express.Router();

/**
 * @swagger
 * /api/v1/user/profile:
 *   get:
 *     tags:
 *       - Profile
 *     summary:
 *     responses:
 *       200:
 *         description:
 */
// router.route("/").get(UserController.getProfile);
